name: bluebuild
on:
  workflow_dispatch: # allow manually triggering builds

permissions: {}

concurrency:
  # only run one build at a time
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true

jobs:
  bluebuild:
    runs-on: ubuntu-24.04
    permissions:
      packages: write
    steps:
      - name: Build image with test CLI
        uses: hastd/blue-build-github-action@test
        with:
          cli_version: post-build-hook
          recipe: recipe.yml
          cosign_private_key: ${{ secrets.SIGNING_SECRET }}
          registry_token: ${{ github.token }}
          pr_event_number: ${{ github.event.number }}
          squash: true
          build_chunked_oci: true
          max_layers: 64
          retry_push_count: 3
      - name: Print manifest (local)
        shell: bash
        run: |
          podman unshare skopeo inspect -n containers-storage:ghcr.io/hastd/bbtest:latest
      - name: Print manifest (remote)
        shell: bash
        run: |
          skopeo inspect -n docker://ghcr.io/hastd/bbtest:latest
